<?xml version="1.0" encoding="iso-8859-1"?>
<!-- EN-Revision: 1.3 Maintainer: tom Status: ready -->
<sect1 id="install.openbsd">
 <title>Unix/OpenBSD Installation</title>
 <para>
  Dieser Abschnitt enthält Anmerkungen und Hinweise zur Installation von
  PHP auf <ulink url="&url.openbsd;">OpenBSD 3.2</ulink>.
 </para>
 <sect2 id="install.openbsd.packages">
  <title>Binärpakete verwenden</title>
   <simpara>
    Die Installation von PHP auf OpenBSD mittels Binärpaketen ist die
    empfohlene und auch einfachste Methode. Das Kernpaket wurde von den
    verschiedenen Modulen getrennt, wodurch jedes Paket unabhängig von den
    anderen installiert und entfernt werden kann. Die nötigen Dateien finden
    Sie auf Ihrer OpenBSD CD oder auf der FTP Site.
   </simpara>
   <simpara>
    Das zur Installation benötigte Hauptpaket ist
    <filename>php4-core-4.2.3.tgz</filename>, welches das Basisprogramm (plus
    gettext und iconv) enthält. Achten Sie als nächstes auf die Modulpakete, wie
    <filename>php4-mysql-4.2.3.tgz</filename> oder
    <filename>php4-imap-4.2.3.tgz</filename>. Sie müssen das
    <command>phpxs</command> Kommando verwenden, um diese Module in Ihrer
    &php.ini; zu aktivieren bzw. zu deaktivieren.
   </simpara>
   <example id="install.openbsd.ports.example">
    <title>OpenBSD Paket Installationsbeispiel</title>
    <programlisting>
<![CDATA[
# pkg_add php4-core-4.2.3.tgz
# /usr/local/sbin/phpxs -s
# cp /usr/local/share/doc/php4/php.ini-recommended /var/www/conf/php.ini
  (in mysql hinzufügen)
# pkg_add php4-mysql-4.2.3.tgz
# /usr/local/sbin/phpxs -a mysql
  (in imap hinzufügen)
# pkg_add php4-imap-4.2.3.tgz
# /usr/local/sbin/phpxs -a imap
  (mysql als Test entfernen)
# pkg_delete php4-mysql-4.2.3
# /usr/local/sbin/phpxs -r mysql
  (Installation der PEAR Bibliotheken)
# pkg_add php4-pear-4.2.3.tgz
]]>
    </programlisting>
   </example>
   <simpara>
    Weitere Informationen über Binärpakete auf OpenBSD finden Sie in
    der manpage <ulink url="&url.openbsd.packages;">packages(7)</ulink>.
   </simpara>
 </sect2>
 <sect2 id="install.openbsd.ports">
  <title>Ports verwenden</title>
  <simpara>
   Sie können PHP auch unter Verwendung des <ulink
   url="&url.openbsd.ports;">ports tree</ulink> kompilieren. Dies wird jedoch
   nur Benutzern empfohlen, die mit OpenBSD vertraut sind. Der PHP4 Port ist
   in drei Unterverzeichnisse aufgeteilt: core, extensions und pear. Das
   Verzeichnis extensions generiert Sub-Pakete für alle unterstützten PHP
   Module. Wenn Sie keines dieser Module erstellen möchten, verwenden Sie den
   <command>no_*</command> FLAVOR. Um z.B. den Build des imap Moduls zu
   verhindern, setzen Sie den FLAVOR auf <command>no_imap</command>.
  </simpara>
 </sect2>
 <sect2 id="install.openbsd.older">
  <title>Ältere Versionen</title>
  <simpara>
   Ältere Versionen von OpenBSD verwendeten das FLAVORS System, um PHP statisch
   verlinkt zu kompilieren. Da es schwer ist, Binärpakete mit dieser Methode zu
   erstellen, wird von dieser Methode abgeraten. Wenn Sie möchten, können Sie
   die alten stabilen Ports Trees weiterhin verwenden, jedoch werden diese von
   dem OpenBSD Team nicht unterstützt. Sollten Sie dazu Anmerkungen haben: Der
   derzeitige Verantwortliche für den Port ist <ulink
   url='mailto:avsm@openbsd.org'>Anil Madhavapeddy</ulink>.
  </simpara>
 </sect2>
</sect1>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

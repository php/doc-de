<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./de/functions/network.xml, last change in rev 1.2 -->
<!-- last change to 'fsockopen' in en/ tree in rev 1.2 -->
<!-- EN-Revision: 1.2 Maintainer: tschuer Status: ready -->
<!-- OLD-Revision: 1.26/EN.1.2 -->
  <refentry id="function.fsockopen">
   <refnamediv>
    <refname>fsockopen</refname>
    <refpurpose>
     Stellt eine Internet- oder Unix-Domain-Socket-Verbindung her
    </refpurpose> 
   </refnamediv>
   <refsect1>
    <title>Beschreibung:</title>
     <methodsynopsis>
      <type>int</type><methodname>fsockopen</methodname>
      <methodparam choice="opt"><type>string</type><parameter>udp://hostname</parameter></methodparam>
      <methodparam><type>int</type><parameter>port</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>errno</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>errstr</parameter></methodparam>
      <methodparam choice="opt"><type>double</type><parameter>timeout</parameter></methodparam>
     </methodsynopsis>
    <para>
     Öffnet eine Socket-Verbindung zum Internet (AF_INET, unter
     Verwendung von TCP oder UDP) oder unter Unix (AF_UNIX). Für eine
     Internet-Verbindung wird eine TCP-Socket-Verbindung zu 
     <parameter>hostname</parameter> über den Port
     <parameter>port</parameter> hergestellt. Für eine UDP-Verbindung
     müssen Sie das Protokoll <parameter>udp://hostname</parameter>
     explizit angeben. Bei einer UNIX-Verbindung wird 
     <parameter>hostname</parameter> als Socket-Pfad interpretiert,
     <parameter>port</parameter> muss in diesem Fall 0 sein. Der
     optionale Parameter <parameter>timeout</parameter> kann benutzt
     werden, um eine Fehlerwartezeit (Timeout-Wert) für den
     Verbindungsaufbau (in Sekunden) zu setzen.
    </para>
    <para>
     Die Funktion <function>fsockopen</function> gibt einen Zeiger auf
     eine Datei zurück, der in Verbindung mit anderen Datei-Funktionen
     (wie z.B.  <function>fgets</function>,
     <function>fgetss</function>, <function>fputs</function>,
     <function>fclose</function>, <function>feof</function>) gebraucht
     werden kann.
    </para>
    <para>
     Wenn der Aufruf der Funktion scheitert, gibt er &false; / falsch
     zurück und wenn die optionalen <parameter>errno</parameter> und
     <parameter>errstr</parameter> Argumente vorhanden sind, enthalten
     sie den aktuellen System-Fehler, der beim Aufruf der
     System-Funktion <literal>connect()</literal> (Verbindungsaufbau)
     erscheint bzw. passiert ist. Wenn die erhaltene errno 0 ist und
     die Funktion &false;/falsch zurück gibt, ist dies ein Hinweis, dass
     der Fehler vor dem <literal>>connect()</literal>-Aufruf
     geschah. Das ist ein Problem, dass häufig mit der Initialisierung
     des Sockets zusammen hängt. Beachten Sie bitte, dass das
     <parameter>errno</parameter> und
     <parameter>errstr</parameter>-Argument als Referenz zu übergeben
     sind.
    </para>
    <para>
     Abhängig von der vorhandenen Umgebung kann die Verbindung zu
     einer UNIX-Domäne oder der optionale Verbindungsaufbau-Zeitfehler
     nicht zur Verfügung stehen.
    </para>
    <para>
     Voreingestellt ist der Verbindungsaufbau im Block-Modus. Sie
     können dies auf Nicht-Block-Modus ändern, indem Sie
     <function>socket_set_blocking</function> benutzen.
     <example>
      <title><function>fsockopen</function>-Beispiel:</title>
      <programlisting role="php">
<![CDATA[
$fp = fsockopen ("www.example.com", 80, &amp;$errno, &amp;$errstr, 30);
if (!$fp) {
    echo "$errstr ($errno)&lt;br&gt;\n";
} else {
    fputs ($fp, "GET / HTTP/1.0\r\n\r\n");
    while (!feof($fp)) {
        echo fgets($fp,128);
    }
    fclose($fp);
}
]]>
      </programlisting>
     </example>
     Das folgende Beispiel zeigt, wie das Datum und die Zeit des
     UDP-Service "daytime" (Port 13) von ihrer eigenen Maschine
     empfangen werden kann:
     <example>
      <title>Gebrauch einer UDP-Verbindung:</title>
      <programlisting role="php">
<![CDATA[
<?php
$fp = fsockopen("udp://127.0.0.1", 13, $errno; $errstr);
if (!$fp) {
    echo "Fehler: $errno - $errstr<br>\n";
} else {
    fwrite($fp,"\n");
    echo fread($fp, 26);
    fclose($fp);
}
?>
]]>
      </programlisting>
     </example>
     Siehe auch: <function>pfsockopen</function>, 
     <function>socket_set_blocking</function>,
     <function>socket_set_timeout</function>,
     <function>fgets</function>,
     <function>fgetss</function>, <function>fputs</function>,
     <function>fclose</function> und <function>feof</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.6 $ -->
<!-- EN-Revision: 1.15 Maintainer: hholzgra Status: ready -->
<!-- Purpose: compression -->
<!-- Membership: bundled, pecl -->

<reference xml:id="ref.zip" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Zip File Functionen</title>
 <titleabbrev>Zip</titleabbrev>

 <partintro>
  <section xml:id="zip.intro">
   &reftitle.intro;
   <para>
    Diese Extension ermöglicht den transparenten Lese- und Schreibzugriff
    auf ZIP-komprimierte Archive und die darin enthaltenen Dateien.
   </para>
  </section>

  <section xml:id="zip.requirements">
   &reftitle.required;
   <section xml:id="zip.requirements.old">
    <title>PHP 4</title>
    <para>
     Die mit PHP 4 ausgelieferte Version der Extension benötigt die
     <link xlink:href="&url.zziplib;">ZZIPlib</link> Bibliothek von
     Guido Draheim ab Version 0.10.6 
    </para>
   </section>

   <section xml:id="zip.requirements.new">
    <title>PHP 5.2.0 und später</title>
    <para>
     Die Extension nutzt nun Funktionalität der
     <link xlink:href="&url.zlib;">zlib</link>
     von Jean-loup Gailly und Mark Adler.
    </para>
   </section>
  </section>

  &reference.zip.configure;

  <section xml:id="zip.configuration">
   &reftitle.runtime;
   &no.config;
  </section>

  <section xml:id="zip.resources">
   &reftitle.resources;
   <para>
    Die Zip Extension benutzt zwei Resourcetypen, einen für
    Verzeichnisse innerhalb eines Zip-Archivs und einen für
    die eigentlichen Dateieinträge innerhalb des Archivs.
   </para>
  </section>

  &reference.zip.constants;

  <section xml:id="zip.examples">
   &reftitle.examples;
   <example>
    <title>Erstellen eines Zip-Archivs</title>
    <programlisting role="php">
<![CDATA[
<?php

$zip = new ZipArchive();
$filename = "./test112.zip";

if ($zip->open($filename, ZIPARCHIVE::CREATE)!==TRUE) {
    exit("cannot open <$filename>\n");
}

$zip->addFromString("testfilephp.txt" . time(), "#1 This is a test string added as testfilephp.txt.\n");
$zip->addFromString("testfilephp2.txt" . time(), "#2 This is a test string added as testfilephp2.txt.\n");
$zip->addFile($thisdir . "/too.php","/testfromfile.php");
echo "numfiles: " . $zip->numFiles . "\n";
echo "status:" . $zip->status . "\n";
$zip->close();
?>
]]>
    </programlisting>
   </example>
   <example>
    <title>Ausgabe eines detailierten Archivlistings</title>
    <programlisting role="php">
<![CDATA[
<?php
$za = new ZipArchive();

$za->open('test_with_comment.zip');
print_r($za);
var_dump($za);
echo "numFiles: " . $za->numFiles . "\n";
echo "status: " . $za->status  . "\n";
echo "statusSys: " . $za->statusSys . "\n";
echo "filename: " . $za->filename . "\n";
echo "comment: " . $za->comment . "\n";

for ($i=0; $i<$za->numFiles;$i++) {
    echo "index: $i\n";
    print_r($za->statIndex($i));
}
echo "numFile:" . $za->numFiles . "\n";
?>
]]>
    </programlisting>
   </example>
   <example>
    <title>Zip Stream Wrapper, Auslesen von OpenOffice Metainformationen</title>
    <programlisting role="php">
<![CDATA[
<?php
$reader = new XMLReader();

$reader->open('zip://' . dirname(__FILE__) . '/test.odt#meta.xml');
$odt_meta = array();
while ($reader->read()) {
    if ($reader->nodeType == XMLREADER::ELEMENT) {
        $elm = $reader->name;
    } else {
        if ($reader->nodeType == XMLREADER::END_ELEMENT && $reader->name == 'office:meta') {
            break;
        }
        if (!trim($reader->value)) {
            continue;
        }
        $odt_meta[$elm] = $reader->value;
    }
}
print_r($odt_meta);
?>
]]>
    </programlisting>
   </example>
   <para>
    Das folgende Beispiel benutzt das alte PHP 4 API, es öffnet
    eine Zip-Datei, liest jede einzelne Datei im Archiv und 
    gibt deren Inhalt aus. Die in diesem Beispiel genutzte
    <filename>test2.zip</filename> Datei ist Teil des Testarchivs
    des ZZIPlib Quellcodes.
   </para>
   <example>
    <title>Zip Beispiel</title>
    <programlisting role="php">
<![CDATA[
<?php

$zip = zip_open("/tmp/test2.zip");

if ($zip) {
    while ($zip_entry = zip_read($zip)) {
        echo "Name:               " . zip_entry_name($zip_entry) . "\n";
        echo "Actual Filesize:    " . zip_entry_filesize($zip_entry) . "\n";
        echo "Compressed Size:    " . zip_entry_compressedsize($zip_entry) . "\n";
        echo "Compression Method: " . zip_entry_compressionmethod($zip_entry) . "\n";

        if (zip_entry_open($zip, $zip_entry, "r")) {
            echo "File Contents:\n";
            $buf = zip_entry_read($zip_entry, zip_entry_filesize($zip_entry));
            echo "$buf\n";

            zip_entry_close($zip_entry);
        }
        echo "\n";

    }

    zip_close($zip);

}
?>
]]>
    </programlisting>
   </example>
  </section>
 </partintro>

 &reference.zip.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
